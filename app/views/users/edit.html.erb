<div class="container">
     <% if @user.errors.any?  %>


        <h2>Following errors are prevented the blog from being saved</h2>

        <ul>
            <% @user.errors.full_messages.each do |msg| %>

            <li><%= msg %></li>
            <% end %>
        </ul>
    <% end %>
       <%= render "form" %>
</div>
